#pragma once
#include "DisplayOutput.h"

void NGames(DisplayOutput &Graphics);		// Führt meherere Speiler hintereinander aus, Anzahl der Spiele, die gespielt werden sollen werden vom Nutzer eingegeben
											// Übergabe der DisplayOutput Klassen-Variable Graphics als Referenz, notwendig für die Ausgabe
